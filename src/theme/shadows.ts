import { ms, vs } from "../utils/scale";

export const SHADOWS = {
  base: {
    shadowColor: "#000",
    shadowOffset: { width: 0, height: vs(2) },
    shadowOpacity: 0.1,
    shadowRadius: ms(3),
    elevation: 3,
  },
};